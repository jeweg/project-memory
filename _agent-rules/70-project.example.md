# Project Agent Rules

Copy this file to `40-project.md` or `60-project.md`.

Use a lower number than `50-project-memory.md` for hard project constraints the
agent should see before Project Memory core rules. Use a higher number for ordinary
project/team conventions that extend the Project Memory behavior.

Keep rules short, concrete, and stable.